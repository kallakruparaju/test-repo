version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
hooks:
  BeforeInstall:
    - location: ./requirements
  ApplicationStart:
    - location: ./start
      timeout: 60
      runas: ec2-user
